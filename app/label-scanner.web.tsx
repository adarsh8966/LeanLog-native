/**
 * Web stub — react-native-vision-camera is not available on web.
 * Metro resolves this file instead of label-scanner.tsx when bundling for web.
 */
import { View } from 'react-native';

export default function LabelScannerScreen() {
  return <View />;
}
